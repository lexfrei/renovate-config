# renovate pulls re2 as an optional native dependency, and npm refuses install
# scripts by default. The example checker compiles patterns with RE2 to match
# how Renovate compiles them, and refuses to run without it in CI.
allow-scripts=re2
